function showDetails() {
    try {
        {
            var username = "raj"
            let userAge = 22
        }
        console.log(`username: ${username}`)
        console.log(`userAge: ${userAge}`)
    } catch (e) {
        console.log(e)
    }
}
showDetails()
